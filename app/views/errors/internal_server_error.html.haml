%hr
%h1.text-center There was a problem processing your request!
%br
.text-center
  = image_tag("500error.png", :size => '200X150')
%br
%p
%h4.text-center
  We have been notified of the problem and we are looking into it. Please refresh the page to see if the problem is just temporary or
  = link_to('go back', root_url)
  to the home page.
%br
- if Rails.env.development?
  %h3.text-center= link_to("Show exception information", '#exception', "data-toggle" => "modal", :class =>'btn btn-info btn-large')
  = render 'shared/exception'