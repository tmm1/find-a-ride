.hero-unit
  .text-center
    %h2 
      %small This page is restricted to admins! Please enter the admin password.
    %br
    = form_tag(authorize_admin_path) do
      = password_field_tag 'admin_password', '', :title => 'Enter the admin password', :class => 'text-input'
      %br
      = submit_tag 'Submit', :class => 'btn btn-primary btn-large'
