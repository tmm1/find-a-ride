- unless @ride_requests.empty?
  = render :partial => 'rides_list_table', :locals => {:rides => @ride_requests, :type => :ride_request}
  = will_paginate @ride_requests, :params => {:controller => :rides, :action => :list, :user_id => current_user.id}, :param_name => :ride_request_page, :renderer => RemoteLinkRenderer
- else
  You have no ride requests at the moment.
