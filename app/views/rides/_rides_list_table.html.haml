%table{:class => 'table table-striped'}
  %th Leaving from
  %th Going to
  %th Time
  %th
  - rides.each do |ride|
    - uuid = user_uuid
    %tr{:id => "ride-cell-#{ride.id}"}
      %td= ride.ride_origin.name
      %td= ride.ride_destination.name
      %td= humanize_time(ride.ride_time)
      %td.delete-ride-cell
        - if ride.deletable?
          %a.btn.btn-primary.delete-ride{:id => "delete-ride-#{ride.id}", :href => user_ride_path(current_user, ride)}
            Delete
          %div.confirm-delete{:id => "confirm-delete-#{ride.id}"}
            %a.yes.btn.btn-success{:href => '#'} Sure?
            %a.no.btn.btn-danger{:href => "#"} No
          %div.ride-loader{:id => "ride-loader-#{ride.id}"}
            - image_tag('loader.gif')
        - else
          %span.btn.btn-primary.disabled{"data-content" => "This cannot be deleted as it is pending."} Delete
