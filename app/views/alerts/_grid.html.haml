%h2 You have the following messages
= render :partial => 'shared/alerts_table', :locals => {:alerts => @alerts}
= will_paginate @alerts, :params => {:controller => :alerts, :action => :index, :user_id => current_user.id}, :renderer => RemoteLinkRenderer