- if @paginated_results.empty?
  #no_results
    %span
      Sorry! We could'nt find anything for your search criteria. 
      = link_to('Go back', home_index_path)
      and try again.
- else
  #results
    %span
      Yay! Found
      = pluralize(@paginated_results.total_entries, 'match') 
      for your search.
    %ul
      - @paginated_results.each do |result|
        %li
          = result.full_name
      = will_paginate @paginated_results